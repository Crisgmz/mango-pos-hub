import SettingsPlaceholder from "./SettingsPlaceholder";
import { Layers } from "lucide-react";

export default function SalonesMesas() {
  return <SettingsPlaceholder title="Salones y Mesas" description="Gestión de zonas y mesas del local" icon={Layers} />;
}
